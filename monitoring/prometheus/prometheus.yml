# monitoring/prometheus/prometheus.yml
global:
  scrape_interval: 5s     # 抓取间隔（5 秒抓一次数据）
  evaluation_interval: 5s

scrape_configs:
  - job_name: "autoai-sentiment-api"
    metrics_path: "/metrics"
    static_configs:
      # 在 Docker 网络中，容器间通过服务名通信，因此填写 api:8000
      - targets: ["api:8000"]