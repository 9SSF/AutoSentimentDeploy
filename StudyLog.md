第一个版本会加载三次模型，因为
- 第 1 次打印：当你执行 python3 main.py 时，Python 解释器从上到下执行文件，主进程首先在全局加载了一次模型。
- 第 2 次打印：主进程确认代码没问题后，启动了 Uvicorn 的 reloader（重载检测进程），这个新进程为了能随时监控文件变化，又把代码解析并执行了一次。
- 第 3 次打印：重载进程准备就绪后，真正负责处理网络请求的 server 工作进程（Worker）被启动，它又把代码整体执行了一次。
所以要引入生命周期管理，只在 server 进程启动时加载一次